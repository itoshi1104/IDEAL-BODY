<div>
   <%= link_to user_path(@article.user) do %>
   <%= attachment_image_tag @article.user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg",  size: "100x100" %><br>
   <%= @article.user.name %>
   <% end %>
</div>

<% if @article.user_id == current_user.id %>
<div>
   <%= link_to "編集", edit_article_path(@article) %>
</div>
<div>
   <%= link_to "削除", article_path(@article), method: :delete %>
</div>
<% end %>


<div><%= @article.title %></div>
<div><%= @article.body %></div>


<div> <% if @article.favorited_by?(current_user) %>
    <p>
      <%= link_to article_favorites_path(@article), method: :delete do %>
        ♥<%= @article.favorites.count %> いいね
      <% end %>
    </p>
    <% else %>
    <p>
      <%= link_to article_favorites_path(@article), method: :post do %>
        ♡<%= @article.favorites.count %> いいね
      <% end %>
    </p>
  <% end %>
</div>

<div>
   <h6>コメントする</h6>
   <div>
     <%= form_with(model:[@article, @comment], local: true) do |f| %>
     <%= f.text_area :body, placeholder: "コメントしよう！" %>
     <%= f.submit "送信する" %>
     <% end %>
  </div>
</div>



<div>コメント一覧</div>
<% @article.comments.each do |comment| %>

<%= comment.user.name %>
<%= comment.body %>
<%= comment.created_at.strftime("%Y-%m-%d %H:%M") %>

<% if @article.user_id == current_user.id %>
<%= link_to '削除する', article_comments_path(comment), method: :delete %>
<% end %>

<% end %>


